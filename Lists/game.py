import random

# to use the random key word u have to import it
# when you run it, it will choose random numbers from the list

options = ['rock', 'paper', 'scissors']
computer_choice = random.choice(options)
print(computer_choice)

# random numbers will be generated each time